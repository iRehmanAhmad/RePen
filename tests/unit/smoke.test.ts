import { describe, it, expect } from 'vitest';

describe('Vite & TypeScript Foundation Smoke Test', () => {
  it('should run tests correctly with Vitest', () => {
    expect(1 + 1).toBe(2);
  });
});
